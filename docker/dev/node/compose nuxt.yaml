name: ${DOCKER_DEV_NAME}

services:
  nuxt:
    build:
      context: ../../
      dockerfile: docker/dev/Dockerfile
    volumes:
      - ../../:/app
    ports:
      - '${DOCKER_DEV_NUXT_PORT}:3000'
    command: bash -c "npm install && npm run dev"
    init: true
